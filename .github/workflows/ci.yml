on:
  push:
    branches:
      - main

jobs:
  main:
    runs-on: ubuntu-22.04
    steps:
      # 標準機能
      - uses: actions/checkout@v4

      # Protoをアクティベート
      - uses: moonrepo/setup-toolchain@v0
        with:
          auto-install: true

      # ライブラリのキャッシュ
      - uses: actions/cache@v3
        id: bun-cache
        with:
          path: node_modules
          key: bun-${{ hashFiles('bun.lockb') }}
          restore-keys: bun-

      # ライブラリのインストール(キャッシュに引っかからなかった時のみ)
      - name: Install
        if: steps.bun-cache.outputs.cache-hit != 'true'
        run: bun install --no-save

      # ビルド
      - name: Build
        run: bun run build

      # 環境変数の設定
      - name: Set env
        run: |
          echo \[env.vars\] >> wrangler.toml
          echo VITE_FOO=${{ secrets.VITE_FOO }} >> wrangler.toml
          mv wrangler.toml dist/wrangler.toml

      # デプロイ
      - name: Deploy
        uses: cloudflare/wrangler-action@v3
        with:
          packageManager: bun
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: |
            --help
            pages deploy ./dist --project-name=hono-test-rm
